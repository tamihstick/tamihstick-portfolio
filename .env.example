# Server configuration for the shared portfolio page-visits counter.
SUPABASE_URL=https://your-project.supabase.co
# Legacy anon JWT for the Edge Function's built-in JWT verification.
# This is a public API key; never put a service-role key here.
SUPABASE_ANON_KEY=your-legacy-anon-key
